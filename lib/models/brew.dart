// now we have snapshot of data that is document avaiable
//no we create object of data we want in the form we want

class Brew {
  final String name;
  final String sugars;
  final int strength;

  Brew({this.name, this.sugars, this.strength});
}
